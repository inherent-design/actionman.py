[pytest]
# Define test class pattern to match all test classes
python_classes = Test*
python_files = test_*.py
python_functions = test_*

# Ignore warnings about classes with __init__ constructors
addopts = --no-header --tb=native -p no:warnings
